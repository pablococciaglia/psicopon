import { Component, input } from '@angular/core';

@Component({
  selector: 'app-violet-stripe',
  imports: [],
  templateUrl: './violet stripe.html',
  styleUrl: './violet stripe.css',
})
export class VioletStripe {
  text = input<string>();
}
